[project]
authors = [{name = "sobhanAhmadian", email = "sobhan.ahmadian.moghadam@gmail.com"}]
dependencies = []
name = "binet"
requires-python = ">= 3.11"
version = "0.1.0"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = ["osx-arm64"]

[tool.pixi.pypi-dependencies]
binet = { path = ".", editable = true }

[tool.pixi.tasks]
